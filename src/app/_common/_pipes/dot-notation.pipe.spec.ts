import { DotNotationPipe } from './dot-notation.pipe';

describe('DotNotationPipe', () => {
  it('create an instance', () => {
    const pipe = new DotNotationPipe();
    expect(pipe).toBeTruthy();
  });
});
